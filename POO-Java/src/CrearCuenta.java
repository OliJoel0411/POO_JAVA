
/**
 *
 * @author Oliver
 */
public class CrearCuenta {
    
    public static void main(String[] args) {
        // crear un objeto de tipo cuenta 
        //Tipo - variable(referencia)      - crea una instancia (valor)
        
        // Variable -> 12GJHKR
            Cuenta primeraCuenta = new Cuenta();
        /*
            primeraCuenta.saldo = 1000.0;
            System.out.println("primeraCuenta = " + primeraCuenta.saldo);
            
            // Crear un espacio de memoria para el objeto (ubicacion de memoria)
            // tambien inicializa cada atributo de su objeto.
            new Cuenta();
            
            Cuenta segundaCuenta = new Cuenta();
            segundaCuenta.saldo = 500.0;
            System.out.println("segundaCuenta = " + segundaCuenta.saldo);
            
            
            // ID de memoria de los objetos creados
            System.out.println("ID primera Cuenta " + primeraCuenta);
            System.out.println("ID segunda Cuenta " + segundaCuenta);
            
            // Comparar si tiene la misma direccion de memoria
            if(primeraCuenta == segundaCuenta)
                System.out.println("Son el mismo objeto");
            else    
                System.out.println("Son objetos diferentes");
            // Comparar si tiene el mismo valor 
            if(primeraCuenta.saldo == segundaCuenta.saldo)
                System.out.println("tienen el mismo valor");
            else
                System.out.println("tienen valor diferente");*/
        
    }
    
}
